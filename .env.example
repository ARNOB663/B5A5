# Example environment variables (do NOT commit real secrets)

# MongoDB connection (replace with your URI)
# mongodb_uri=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/mydb?retryWrites=true&w=majority

# Application
# node_env=development
# port=3000

# CORS - comma-separated list of allowed origins (leave unset to allow any)
# cors_origin=http://localhost:3000,https://your-app.com

# Rate limit
rate_limit_window_ms=900000
rate_limit_max_requests=1000

# JWT
# JWT_SECRET=your_jwt_secret
# JWT_EXPIRES_IN=7d
